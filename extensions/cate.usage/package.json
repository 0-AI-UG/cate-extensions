{
  "name": "cate.usage",
  "version": "1.1.0",
  "private": true,
  "description": "Agent usage and cost dashboard for Cate, backed by the ccusage library (MIT).",
  "scripts": {
    "build": "node ./build.mjs",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc -p tsconfig.server.json --noEmit && tsc -p tsconfig.browser.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ccusage": "18.0.11"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
